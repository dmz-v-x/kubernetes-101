apiVersion: v1
kind: Pod
metadata:
  name: env-example
spec:
  containers:
    - name: main-container
      image: nginx
      env:
        - name: APP_ENV
          value: "production"
        - name: DB_HOST
          value: "db.example.com"
        - name: LOG_LEVEL
          value: "info"
